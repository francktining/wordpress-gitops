apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: wordpress
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://charts
    chart: registry-1.docker.io/bitnamicharts
    targetRevision: 24.1.10

    helm:
      values: |
        service:
          type: NodePort

  destination:
    server: https://kubernetes.default.svc
    namespace: wordpress

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
